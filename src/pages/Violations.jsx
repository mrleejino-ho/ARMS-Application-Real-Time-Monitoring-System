import PageHeader from "../components/PageHeader";

export default function Violations() {
  return (
    <PageHeader
      title="Violations"
      description="Review detected restricted application events."
    />
  );
}